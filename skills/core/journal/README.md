# journal

Monthly development journal. Reads git log, merged PRs, decisions, and project memory; produces a structured narrative of what happened in the project that month, then saves it under `.claude/journal/YYYY-MM.md`.

Install:
```bash
jutsu install journal
```

Trigger phrases: "journal", "monthly journal", "what did we do this month", `/journal`, `/journal march`.
