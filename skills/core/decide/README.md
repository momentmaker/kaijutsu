# decide

Decision journal for AI coding agents. Records architectural decisions with reasoning and tripwires; surfaces them later to prevent re-debating and to catch decisions that need revisiting.

Install:
```bash
jutsu install decide
```

Then invoke from your agent:
- `/decide Use Redis for the rate limiter` — record
- `/decide` — list active decisions
- "why did we choose X" — query mode
