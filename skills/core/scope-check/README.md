# scope-check

Mid-task scope mirror. Compares the original task intent (from a plan, task list, or the user) against the actual git diff and presents the contrast as a domain-grouped table — not as judgment.

Install:
```bash
jutsu install scope-check
```

Trigger phrases: "scope check", "am I scope creeping", "how big has this gotten", "check the scope".
