# CLAUDE.md

Claude Code: read [`AGENTS.md`](./AGENTS.md). It is the canonical contributor guidance for any AI agent working on kaijutsu itself, kept in sync across Claude / Codex / Gemini.

The bullet that matters most when in doubt:

> kaijutsu is pre-alpha. Don't add features beyond what the current build stage requires. Don't write code that depends on `jutsu` before Stage 2 lands.

For end-user docs (using the published `jutsu` CLI), see [`README.md`](./README.md) and [`SCHEMA.md`](./SCHEMA.md).
